<Window x:Class="DataUploaderUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataUploaderUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="150" Width="400">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>

            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>

            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Select Country:" Margin="5"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Name="ComboBoxCountry" Margin="5">
            <!-- TODO: checkboxes in dropdown -> "eventueel als uitbreiding moeten ze ook kunnen gesorteerd worden" -->
        </ComboBox>
        <Button Grid.Row="1" Name="ButtonChooseFile" Content="Choose File" Margin="5" Click="ButtonChooseFile_Click"/>
        <TextBox Grid.Row="1" Grid.Column="1" IsReadOnly="True" Name="TextBoxFilePath" Margin="5"/>
        <Button Grid.Row="2" Grid.ColumnSpan="2" Name="ButtonOpenUploader" Content="Open Uploader" Margin="5" Click="ButtonUploader_Click"/>
        
    </Grid>
</Window>
